import App from "../components/App";
import { NextPage } from "next";
import { Container } from "@material-ui/core";

const GroupsPage: NextPage = () => {
  return (
    <App>
      <Container>
        <div style={{ marginBottom: 8, marginTop: 8 }}>調整中</div>
      </Container>
    </App>
  );
};

export default GroupsPage;
